<section style="background-image: url('{{ section.settings.bg_image | image_url }}'); background-size: cover; padding: 80px 20px; text-align: center; color: white;">
  <h1>{{ section.settings.title }}</h1>
  <p>{{ section.settings.subtitle }}</p>
  <a href="{{ section.settings.button_link }}" style="background-color: #ff6600; padding: 10px 20px; color: white; text-decoration: none; border-radius: 8px;">
    {{ section.settings.button_text }}
  </a>
</section>
add hero section
