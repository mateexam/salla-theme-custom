{% if product.metafields.video_url %}
  <div class="video-wrapper">
    <iframe
      width="100%"
      height="315"
      src="{{ product.metafields.video_url }}"
      frameborder="0"
      allowfullscreen>
    </iframe>
  </div>
{% else %}
  <p>لا يوجد فيديو لهذا المنتج حاليًا.</p>
{% endif %}
